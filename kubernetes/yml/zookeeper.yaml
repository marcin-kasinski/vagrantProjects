kind: ConfigMap
metadata:
  name: zookeeper-server-jaas-conf-cm
  namespace: default
apiVersion: v1
data:
  zookeeper_server_jaas.conf: |-
    KafkaServer {
        com.sun.security.auth.module.Krb5LoginModule required
        useKeyTab=true
        storeKey=true
        keyTab="/opt/zookeeper/conf/zk.keytab"
        principal="zookeeper/{HOSTNAME_FQDN}@KAFKA.SECURE";
    };
---
apiVersion: v1
kind: Service
metadata:
  name: zk-hs
  labels:
    app: zookeeper
spec:
  ports:
  - port: 2888
    name: server
  - port: 3888
    name: leader-election
  - port: 2181
    name: client    
  - port: 8080
    name: jmxagent
  clusterIP: None
  selector:
    app: zookeeper
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: zk
spec:
  serviceName: "zk-hs"
  replicas: 3
  updateStrategy:
    type: RollingUpdate
  podManagementPolicy: Parallel
  selector:
    matchLabels:
      app: zookeeper
  template:
    metadata:
      labels:
        app: zookeeper
        kubernetes.io/name: "zookeeper"
        tier: backend
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/scheme: "http"
        prometheus.io/path: "/"
        prometheus.io/port: "8080"
    spec:
      containers:
      - name: zookeeper
        image: marcinkasinski/zookeepercluster
        #imagePullPolicy: Always
        imagePullPolicy: Never
        ports:
        - containerPort: 2181
          name: client
        - containerPort: 2888
          name: server
        - containerPort: 3888
          name: leader-election
        env:
        - name: "JAAS_FILE_LOCATION"
          value: "/opt/zookeeper/conf/zookeeper_server_jaas.conf"
        - name: "JAAS_FILE_LOCATION_RO"
          value: "/opt/zookeeper/conf/zookeeper_server_jaas.conf_RO"        
        - name: "ZOOKEEPER_NODES"
          value: "server.1=zk-0.zk-hs.default.svc.cluster.local:2888:3888;server.2=zk-1.zk-hs.default.svc.cluster.local:2888:3888;server.3=zk-2.zk-hs.default.svc.cluster.local:2888:3888"
        #ustawia na wartość default 
        - name: "ZOOKEEPER_PARAM_tickTime"
          value: "2000"
        #ustawia na wartość default 
        - name: "ZOOKEEPER_PARAM_maxClientCnxns"
          value: "60"
        volumeMounts:
        - mountPath: /datalog/zookeeper
          name: zookeeper-logs
        - mountPath: /data/zookeeper
          name: zookeeper-data
        - name: config-volume-kafka-0-service-keytab
          mountPath: /opt/kafka/config/kafka-0.service.keytab
          subPath: kafka-0.service.keytab
        - name: config-volume-kafka-1-service-keytab
          mountPath: /opt/kafka/config/kafka-1.service.keytab
          subPath: kafka-1.service.keytab
        - name: config-volume-kafka-2-service-keytab
          mountPath: /opt/kafka/config/kafka-2.service.keytab
          subPath: kafka-2.service.keytab
        - name: config-volume-zookeeper-server-jaas-conf-cm
          mountPath: /opt/zookeeper/conf/zookeeper_server_jaas.conf_RO
          subPath: zookeeper_server_jaas.conf
      volumes:
        - name: config-volume-krb5-conf-cm
          configMap:
            name: zookeeper-krb5-conf-cm
        - name: config-volume-zookeeper-0-service-keytab
          configMap:
            name: zookeeper-0-service-keytab
        - name: config-volume-zookeeper-1-service-keytab
          configMap:
            name: zookeeper-1-service-keytab
        - name: config-volume-zookeeper-2-service-keytab
          configMap:
            name: zookeeper-2-service-keytab      
        - name: config-volume-zookeeper-server-jaas-conf-cm
          configMap:
            name: zookeeper-server-jaas-conf-cm      
        - name: zookeeper-logs
          emptyDir: {}
        - name: zookeeper-data
          emptyDir: {}
